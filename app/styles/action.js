import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  action: {
    width: 75,
    height: 75,
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 4,
    borderRadius: 8,
    borderColor: '#767686',
  }
})
